#!/bin/sh

# TODO link redis container to evie
#docker build -t ericychoi/redis docker/redis

docker build -t ericychoi/evie .
docker rm -f evie 2>/dev/null
docker run -d -p 55555:55555 --name evie ericychoi/evie
